apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.app.name }}
  name: {{ .Chart.Name }}
spec:
  ports:
    - name: "8080"
      port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    app: {{ .Values.app.name }}
