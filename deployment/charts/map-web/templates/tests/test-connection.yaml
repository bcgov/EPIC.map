apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "map-web.fullname" . }}-test-connection"
  labels:
    {{- include "map-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "map-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
